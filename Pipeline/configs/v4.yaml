extends: base.yaml

paths:
  session_root: "experiments/v4"

num_cpu: 16

env:
  module: "red_gym_env_lstm"
  class: "RedGymEnvLSTM"

model:
  type: "RecurrentPPOLD"
  policy: "MultiInputLstmPolicyLD"
  ld_coef: 0.1
