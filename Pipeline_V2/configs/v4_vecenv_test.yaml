extends: v4.yaml

num_cpu: 16

paths:
  session_root: "experiments/v4_vecenv_test"

env:
  max_steps: 4096
  print_rewards: false
  send_map_to_agent: false

model:
  n_steps: 256
  ld_coef: 0.1

total_timesteps: 5e6
save_freq: 5000

logging:
  save_freq: 5000
