extends: v1.yaml

num_cpu: 16

paths:
  session_root: "experiments/v1_reset_test"

env:
  debug_reset_timing: true
  max_steps: 4096
  print_rewards: false

model:
  n_steps: 128

total_timesteps: 5e5
save_freq: 4096

logging:
  save_freq: 10000
